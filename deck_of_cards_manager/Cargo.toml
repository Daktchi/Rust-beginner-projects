[package]
name = "deck_of_cards_manager"
version = "0.1.0"
edition = "2024"

#Cargo build --offline do not use network at all and use my local pre-downlaod crates that exist localy
[dependencies]
rand = "0.8.5"

